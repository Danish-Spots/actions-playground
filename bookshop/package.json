{
  "name": "bookshop",
  "version": "1.0.0",
  "dependencies": {
    "@sap/cds": "^9"
  },
  "devDependencies": {
    "@cap-js/cds-test": "^1",
    "@cap-js/sqlite": "^2.4",
    "jest": "^29.7.0",
    "jest-junit": "^17.0.0"
  },
  "scripts": {
    "start": "cds-serve",
    "test": "cds bind --exec -- jest"
  },
  "private": true
}
