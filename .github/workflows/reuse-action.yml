on:
  push:

jobs:
  reuse-action:
    runs-on: ubuntu-latest
    steps:
      - name: Reuse custom action
        id: reuse-action-step
        uses: Danish-Spots/private-action@v1
        with:
          who-to-greet: 'DS'
      - name: Get output time
        run: echo "The time was  $ {{ steps.reuse-action-step.outputs.time }}."
